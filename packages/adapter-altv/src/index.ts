/**
 * ALT:V adapter entry point.
 * Depends on core contracts to provide a typed integration layer
 * between ALT:V WebView events and the shared bridge protocol.
 */
export const placeholder = 'altv';
