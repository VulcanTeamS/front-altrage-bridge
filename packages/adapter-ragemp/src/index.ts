/**
 * RageMP adapter entry point.
 * Depends on core contracts to provide a typed integration layer
 * between RageMP browser events and the shared bridge protocol.
 */
export const placeholder = 'ragemp';
