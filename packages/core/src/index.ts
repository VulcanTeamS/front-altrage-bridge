/**
 * Entry point for the Altrage bridge core module.
 * The core package exposes shared contracts, event schemas, and utilities
 * that are consumed by platform-specific adapters (ALT:V, RageMP, etc.).
 */
export const placeholder = true;
